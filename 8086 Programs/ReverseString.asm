ASSUME CS:CODE, DS:DATA

DATA SEGMENT
	ORG 1000h
	STRING DB 72,69,76,76,79
	ORG 2000h
	REV DB 5 DUP(0)
DATA ENDS

CODE SEGMENT
START:	MOV AX, DATA
		MOV DS, AX
		MOV ES, AX

		MOV CX, 0005H
		LEA SI, STRING+5-1
		LEA DI, REV
				
RLOOP:	STD
		LODSB
		CLD
		STOSB
		DEC CX
		JNZ RLOOP

		MOV AH, 4Ch
		INT 21h
CODE ENDS
END START