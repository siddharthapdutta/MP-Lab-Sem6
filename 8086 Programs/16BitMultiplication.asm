ASSUME CS:CODE, DS:DATA

DATA SEGMENT
	ORG 1000h
	NUM1 DW -10
	NUM2 DW 06
	RES DW 2 DUP(0)
DATA ENDS

CODE SEGMENT
START:	MOV AX, DATA
	MOV DS, AX
	MOV AX, NUM1
	MOV BX, NUM2
	IMUL BX
	MOV RES, AX
	MOV RES+2, DX
	MOV AH, 4Ch
	INT 21h
CODE ENDS
END START