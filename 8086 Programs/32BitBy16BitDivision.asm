ASSUME CS:CODE, DS:DATA

DATA SEGMENT
	ORG 1000h
	NUM DD -12345
	DEN DW 20
	RES DW 2 DUP(0)
DATA ENDS

CODE SEGMENT
START:	MOV AX, DATA
	MOV DS, AX

	LEA SI, NUM
	MOV AX, [SI]
	MOV DX, [SI+2]
	IDIV DEN
	MOV RES, AX
	MOV RES+2, DX

	MOV AH, 4Ch
	INT 21h
CODE ENDS
END START